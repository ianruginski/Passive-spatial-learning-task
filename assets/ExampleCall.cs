//using (StreamWriter writerExample = new StreamWriter(Example)) { // Here is the instatiation of your writer class. Do this in you script where you need the writer!
			
///		number = UnityEngine.Random.Range (1,4); // Just generates a random number between 1-4
//		writerPrnum.WriteLine(number); // Write this number in the specific file above (Example).
