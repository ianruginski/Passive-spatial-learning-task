using UnityEngine;
using System.Collections;

public class scr_SetRenderQueue : MonoBehaviour {

	// Use this for initialization
	void Start () {
		renderer.material.renderQueue=2800;
		}
	
	// Update is called once per frame
	void Update () {
	
	}
}
